{
  "name": "http-server",
  "version": "1.0.0",
  "description": "golang server",
  "author": "Lequeston",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "cross-env NODE_ENV=development go run server.go",
    "build": "cross-env NODE_ENV=production go build"
  },
  "devDependencies": {
    "cross-env": "^7.0.2"
  }
}
